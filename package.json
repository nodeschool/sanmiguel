{
  "name": "nodeschoolsm",
  "version": "0.2.0",
  "private": false,
  "scripts": {
    "start":"cd website && yarn start",
    "build": "cd website && yarn install && yarn build"
  },
  "dependencies": {
    "moment": "^2.22.2",
    "yargs": "^12.0.5"
  },
  "license": "MIT"
}
